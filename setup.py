#!/usr/bin/env python

from setuptools import setup

setup(
    name='example-bar',
    version='1.0.0',
    url='https://github.com/beck/sandbox/tree/example-bar',
    install_requires=[
        "simplejson < 3"
    ]
)
