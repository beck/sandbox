{
  "name": "ampersand-building",
  "version": "0.0.1",
  "dependencies": {
    "ampersand-view": "^9.0.2"
  },
  "scripts": {
    "build": "browserify index.js > bundle.js; js-size bundle.js",
    "build:c": "browserify -p bundle-collapser/plugin index.js > bundle.c.js; js-size bundle.c.js",
    "build:min": "browserify -p bundle-collapser/plugin index.js | uglifyjs > bundle.min.js; js-size bundle.min.js",
    "build:all": "npm run build; npm run build:c; npm run build:min"
  },
  "devDependencies": {
    "browserify": "^14.3.0",
    "bundle-collapser": "^1.2.1",
    "uglify-js": "^3.0.10"
  }
}
